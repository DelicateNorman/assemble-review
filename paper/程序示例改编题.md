# 程序示例改编填空题

## 一、顺序程序设计

1. 下列程序段显示字符串信息，请将程序填写完整：

.DATA
STRING DB 'Hello, Everybody!', 0DH, 0AH, '$'

.CODE
.STARTUP
MOV DX, ①_
MOV AH, ②_
INT 21H
.EXIT 0
END

2. 下面程序段执行后，AX的值是 __3__ 。

.CODE
.STARTUP
MOV AX, 1234H
ADD AX, 5678H
.EXIT 0
END

## 二、分支程序设计

3. 下列程序段计算AX的绝对值，请将程序填写完整：

.DATA
RESULT DW ?

.CODE
.STARTUP
MOV AX, -1234H
CMP AX, 0
④_ nonneg
NEG AX
nonneg:
MOV RESULT, AX
.EXIT 0
END

4. 下面程序段执行后，如果AX初始值为15，则AX最终值为 __5__ ；如果AX初始值为10，则AX最终值为 __6__ 。

.CODE
.STARTUP
MOV AX, 15
TEST AX, 01H
⑦_ iseven
ADD AX, 1
iseven:
RCR AX, 1
.EXIT 0
END

5. 下列程序段实现符号判断，请将程序填写完整：

.DATA
POSMSG DB 'Positive', 0DH, 0AH, '$'
NEGMSG DB 'Negative', 0DH, 0AH, '$'
ZERO DB 'Zero', 0DH, 0AH, '$'

.CODE
.STARTUP
MOV AX, -100
CMP AX, 0
⑧_ positive
JL ⑨_
MOV DX, OFFSET ZERO
JMP display
positive:
MOV DX, OFFSET POSMSG
JMP display
negative:
MOV DX, OFFSET NEGMSG
display:
MOV AH, ⑩_
INT 21H
.EXIT 0
END

## 三、循环程序设计

6. 下列程序段计算1到100的自然数之和，请将程序填写完整：

.DATA
SUM DW ?

.CODE
.STARTUP
⑪_ AX, AX
MOV CX, 100
AGAIN:
ADD AX, CX
⑫_ AGAIN
MOV SUM, ⑬_
.EXIT 0
END

7. 下面程序段求数组元素的平均值，请将程序填写完整：

.DATA
ARRAY DW 10, 20, 30, 40, 50
COUNT EQU 5
AVERAGE DW ?

.CODE
.STARTUP
MOV CX, COUNT
MOV BX, OFFSET ARRAY
XOR AX, AX
XOR DX, DX
SUM_LOOP:
ADD AX, [BX]
ADC DX, 0
⑭_ BX, 2
⑮_ SUM_LOOP
MOV BX, COUNT
DIV BX
MOV AVERAGE, AX
.EXIT 0
END

8. 下列程序段执行后，SUM的值为 __16__ 。

.DATA
SUM DW 0

.CODE
.STARTUP
MOV CX, 5
MOV BX, 1
CALC_LOOP:
ADD SUM, BX
ADD BX, 2
LOOP CALC_LOOP
.EXIT 0
END

## 四、子程序设计

9. 下列程序段实现回车换行子程序，请将程序填写完整：

.CODE
.STARTUP
CALL ⑰_
.EXIT 0

DPCROLF PROC NEAR
⑱_ AX
PUSH DX
MOV DL, 0DH
MOV AH, ⑲_
INT 21H
MOV DL, 0AH
INT 21H
POP DX
POP AX
⑳_
DPCROLF ENDP

END

10. 下面程序段计算阶乘，请将程序填写完整：

.DATA
RESULT DW ?

.CODE
.STARTUP
MOV AX, 5
CALL FACTORIAL
MOV RESULT, AX
.EXIT 0

FACTORIAL PROC NEAR
CMP AX, 1
JLE ㉑_
PUSH BX
MOV BX, AX
DEC AX
CALL FACTORIAL
MUL BX
POP BX
㉒_
FACTORIAL ENDP

END

11. 下列程序段执行后，RESULT的值为 __23__ 。

.DATA
RESULT DW 0

.CODE
.STARTUP
MOV AX, 4
MOV BX, 3
CALL ADD_PROC
MOV RESULT, AX
.EXIT 0

ADD_PROC PROC NEAR
ADD AX, BX
RET
ADD_PROC ENDP

END

12. 为了实现参数传递，请将下面代码补充完整：

.DATA
NUM1 DW 100
NUM2 DW 200
SUM DW ?

.CODE
.STARTUP
MOV AX, NUM1
PUSH AX
MOV AX, NUM2
㉔_ AX
CALL ADD_TWO
MOV SUM, AX
.EXIT 0

ADD_TWO PROC NEAR
PUSH BP
MOV BP, SP
MOV AX, [BP+㉕_]
ADD AX, [BP+6]
POP BP
㉖_
ADD_TWO ENDP

END

## 答案表格

| 题号 | 答案 |
|------|------|
| ① | OFFSET STRING |
| ② | 9 |
| ③ | 68ACH (1234H + 5678H = 68ACH) |
| ④ | JNS |
| ⑤ | 8 (15+1=16, 16/2=8) |
| ⑥ | 5 (10/2=5) |
| ⑦ | JZ |
| ⑧ | JG |
| ⑨ | negative |
| ⑩ | 9 |
| ⑪ | XOR |
| ⑫ | LOOP |
| ⑬ | AX |
| ⑭ | ADD |
| ⑮ | LOOP |
| ⑯ | 25 (1+3+5+7+9=25) |
| ⑰ | DPCROLF |
| ⑱ | PUSH |
| ⑲ | 2 |
| ⑳ | RET |
| ㉑ | END_PROC |
| ㉒ | RET |
| ㉓ | 7 (4+3=7) |
| ㉔ | PUSH |
| ㉕ | 4 |
| ㉖ | RET 4 |
# 程序示例改编填空题

# 二、填空题 (每空1分，共20分)

# (将正确答案按照序号填写到下表中)

<table><tr><td>①</td><td>②</td><td>③</td><td>④</td><td>⑤</td></tr><tr><td></td><td></td><td></td><td></td><td></td></tr><tr><td>⑥</td><td>⑦</td><td>⑧</td><td>⑨</td><td>⑩</td></tr><tr><td></td><td></td><td></td><td></td><td></td></tr><tr><td>⑪</td><td>⑫</td><td>⑬</td><td>⑭</td><td>⑮</td></tr><tr><td></td><td></td><td></td><td></td><td></td></tr><tr><td>⑯</td><td>⑰</td><td>⑱</td><td>⑲</td><td>⑳</td></tr><tr><td></td><td></td><td></td><td></td><td></td></tr></table>

1. 下列程序段显示字符串信息，请将程序填写完整：

```txt
.DATA
STRING DB 'Hello, Everybody!', 0DH, 0AH, '$'

.CODE
.STARTUP
MOV DX, ①_
MOV AH, ②_
INT 21H
.EXIT 0
END
```

2. 下面程序段执行后，AX的值是 __③__ 。

```txt
.CODE
.STARTUP
MOV AX, 1234H
ADD AX, 5678H
.EXIT 0
END
```

3. 下列程序段计算AX的绝对值，请将程序填写完整：

```txt
.DATA
RESULT DW ?

.CODE
.STARTUP
MOV AX, -1234H
CMP AX, 0
④_ nonneg
NEG AX
nonneg:
MOV RESULT, AX
.EXIT 0
END
```

4. 下面程序段执行后，如果AX初始值为15，则AX最终值为 __⑤__ ；如果AX初始值为10，则AX最终值为 __⑥__ 。

```txt
.CODE
.STARTUP
MOV AX, 15
TEST AX, 01H
⑦_ iseven
ADD AX, 1
iseven:
RCR AX, 1
.EXIT 0
END
```

5. 下列程序段实现符号判断，请将程序填写完整：

```txt
.DATA
POSMSG DB 'Positive', 0DH, 0AH, '$'
NEGMSG DB 'Negative', 0DH, 0AH, '$'
ZERO DB 'Zero', 0DH, 0AH, '$'

.CODE
.STARTUP
MOV AX, -100
CMP AX, 0
⑧_ positive
JL ⑨_
MOV DX, OFFSET ZERO
JMP display
positive:
MOV DX, OFFSET POSMSG
JMP display
negative:
MOV DX, OFFSET NEGMSG
display:
MOV AH, ⑩_
INT 21H
.EXIT 0
END
```

6. 下列程序段计算1到100的自然数之和，请将程序填写完整：

```txt
.DATA
SUM DW ?

.CODE
.STARTUP
⑪_ AX, AX
MOV CX, 100
AGAIN:
ADD AX, CX
⑫_ AGAIN
MOV SUM, ⑬_
.EXIT 0
END
```

7. 下面程序段求数组元素的平均值，请将程序填写完整：

```txt
.DATA
ARRAY DW 10, 20, 30, 40, 50
COUNT EQU 5
AVERAGE DW ?

.CODE
.STARTUP
MOV CX, COUNT
MOV BX, OFFSET ARRAY
XOR AX, AX
XOR DX, DX
SUM_LOOP:
ADD AX, [BX]
ADC DX, 0
⑭_ BX, 2
⑮_ SUM_LOOP
MOV BX, COUNT
DIV BX
MOV AVERAGE, AX
.EXIT 0
END
```

8. 下列程序段执行后，SUM的值为 __16__ 。

```txt
.DATA
SUM DW 0

.CODE
.STARTUP
MOV CX, 5
MOV BX, 1
CALC_LOOP:
ADD SUM, BX
ADD BX, 2
LOOP CALC_LOOP
.EXIT 0
END
```

9. 下列程序段实现回车换行子程序，请将程序填写完整：

```txt
.CODE
.STARTUP
CALL ⑰_
.EXIT 0

DPCROLF PROC NEAR
⑱_ AX
PUSH DX
MOV DL, 0DH
MOV AH, ⑲_
INT 21H
MOV DL, 0AH
INT 21H
POP DX
POP AX
⑳_
DPCROLF ENDP

END
```

10. 下面程序段计算阶乘，请将程序填写完整：

```txt
.DATA
RESULT DW ?

.CODE
.STARTUP
MOV AX, 5
CALL FACTORIAL
MOV RESULT, AX
.EXIT 0

FACTORIAL PROC NEAR
CMP AX, 1
JLE ㉑_
PUSH BX
MOV BX, AX
DEC AX
CALL FACTORIAL
MUL BX
POP BX
㉒_
FACTORIAL ENDP

END
```

11. 下列程序段执行后，RESULT的值为 __23__ 。

```txt
.DATA
RESULT DW 0
.CODE
.STARTUP
MOV AX, 4
MOV BX, 3
CALL ADD_PROC
MOV RESULT, AX
.EXIT 0

ADD_PROC PROC NEAR
ADD AX, BX
RET
ADD_PROC ENDP

END
```

12. 为了实现参数传递，请将下面代码补充完整：

```txt
.DATA
NUM1 DW 100
NUM2 DW 200
SUM DW ?

.CODE
.STARTUP
MOV AX, NUM1
PUSH AX
MOV AX, NUM2
㉔_ AX
CALL ADD_TWO
ADD SP,4
MOV SUM, AX
.EXIT 0

ADD_TWO PROC NEAR
PUSH BP
MOV BP, SP
MOV AX, [BP+㉕_]
ADD AX, [BP+6]
POP BP
㉖_
ADD_TWO ENDP

END
```

---

# 参考答案

<table><tr><td>①</td><td>②</td><td>③</td><td>④</td><td>⑤</td></tr><tr><td>OFFSET STRING</td><td>9</td><td>68ACH</td><td>JNS</td><td>8</td></tr><tr><td>⑥</td><td>⑦</td><td>⑧</td><td>⑨</td><td>⑩</td></tr><tr><td>5</td><td>JZ</td><td>JG</td><td>negative</td><td>9</td></tr><tr><td>⑪</td><td>⑫</td><td>⑬</td><td>⑭</td><td>⑮</td></tr><tr><td>XOR</td><td>LOOP</td><td>AX</td><td>ADD</td><td>LOOP</td></tr><tr><td>⑯</td><td>⑰</td><td>⑱</td><td>⑲</td><td>⑳</td></tr><tr><td>25</td><td>DPCROLF</td><td>PUSH</td><td>2</td><td>RET</td></tr><tr><td>㉑</td><td>㉒</td><td>㉓</td><td>㉔</td><td>㉕</td></tr><tr><td>BASE_CASE</td><td>RET</td><td>7</td><td>PUSH</td><td>4</td></tr><tr><td>㉖</td></tr><tr><td>RET 4</td></tr></table>